Write a C program to print numbers from 1 to 10 and 10 to 1 using a do-while loop.

solution : 

#include <stdio.h>

int main(){
    int count = 0;

    do{
        printf("%d\n",count + 1);
        count ++;
    }while(count <= 9);
    do{
        printf("%d\n", count - 1);
        count -- ;
    }while(count >= 2);


    return 0;
}
